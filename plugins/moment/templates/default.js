;(function (global, factory) {
    typeof exports === 'object' && typeof page !== 'undefined' ? page.exports = factory() :
    typeof define === 'function' && define.amd ? define(factory) :
    global.moment = factory()
}(this, function () { 'use strict';
